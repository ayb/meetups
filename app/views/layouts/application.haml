!!!
%html{ lang: "en" }
  %head
    %title
      = yield :title

    = javascript_include_tag :application
    = stylesheet_link_tag :application
    = csrf_meta_tag

  %body
    .container
      - flash.each do |flash_type, message|
        %div{ class: "flash #{flash_type}" }= message

      = yield
